#version 330 core

in vec3 world_position;
out vec4 color;

void main(){
	color.xyz = world_position;
	// world position is inside a 1 radius sphere, but color need to be inside (0,1)
	color.xyz = 0.5f * (vec3(1.0f) + color.xyz);
	color.w = 1.0f;
}